버전 ID: 34f591016aed411e3aedd2d55dbb57943e1f2939

# 목표
- "이미지 입력 경로 확장" 기능 중 경로 1(클립보드 붙여넣기, Ctrl+V) 구현.
- 기존 파일 업로드 검증 로직(PNG/JPEG 형식 검사 및 Image 객체 디코딩 검증)을 재사용하여, 문구 입력창 밖에서 클립보드의 이미지 붙여넣기 시 배경 이미지로 반영되도록 처리.

# 현재 상태
- 경로 1(붙여넣기, CHK-01~05) 구현 완료.
- `js/editor-state.js` 내 업로드 처리 로직을 `handleImageFile(file)` 공통 함수로 분리.
- `js/shortcuts.js`에서 슬라이더/버튼 등 비-텍스트 입력 요소에 포커스가 맞춰진 상태에서도 Ctrl+V 및 Ctrl+Z가 정상 동작하도록 `isTextInputElement` 판정 로직 보완.
- 경로 2(드래그앤드롭, CHK-06~10)는 구현되지 않은 상태.

# 실행 명령
- 별도의 빌드 도구나 패키지 설치 과정이 필요 없으며(설치 필요 없음), 브라우저에서 `index.html` 파일을 직접 열어서 실행 및 검사할 수 있습니다.

# 통과 검사
- CHK-01 (통과 예상)
- CHK-02 (통과 예상)
- CHK-03 (통과 예상)
- CHK-04 (통과 예상)
- CHK-05 (통과 예상)

# 남은 문제
- 경로 2(드래그앤드롭, CHK-06~10) 미구현:
  - 업로드 영역(드롭존) 대상 dragover, dragleave, drop 이벤트 핸들러 구현 필요.
  - 드롭 이벤트 발생 시 한 번에 여러 장의 파일이 들어오면 "한 번에 한 장만 가능" 안내 출력 처리 필요.
  - 이미지 이외의 파일(`test.txt` 등) 드롭 시 오류 안내 출력 및 기존 배경 유지 처리 필요.
  - 드래그 진입/이탈 시 드롭존 스타일(보라색 테두리 강조 등) 반영 필요.

# 다음 행동
- 다음 세션에서 경로 2(드래그앤드롭, CHK-06~10) 구현 진행.
- `js/editor-state.js`의 `handleImageFile(file)` 공통 함수 및 드롭존 영역을 활용하여 드래그앤드롭 이벤트 수신 및 다중 파일/비이미지 파일 예외 처리 추가.

# 건드리지 말 것
- `tests/checks.md` 파일은 검사 규칙 파일이므로 절대 수정 및 삭제하지 마십시오.
- 기존 모듈 구조(`js/` 디렉터리 내 파일 분리)를 변경하지 마십시오.
